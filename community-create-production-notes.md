# فحص إتاحة إنشاء المجتمع في الإنتاج

## الفحص الأول بعد النشر

في 23 أغسطس 2026 عند نحو 04:07، فُتح المسار المنشور التالي في متصفح ضيف مستقل:

`https://yemnacommuni-mhbuucvz.manus.space/communities/create`

أعاد التطبيق صفحة «هذه الصفحة غير متاحة» بدلاً من حراسة الضيف أو نموذج الإنشاء، بينما بقي المسار العام `/communities` مستجيباً ويعرض قائمة مجتمعات فارغة بصدق. جرى الفحص مباشرةً بعد حفظ الإصدار `dc397e07`، لذلك لا يثبت وحده أن كود المسار معطّل؛ يجب إعادة الفحص بعد اكتمال انتشار الإصدار قبل توجيه المستخدم إلى مسار الإنشاء أو تطبيق تعديل جديد.

لا أُنشئت أو عُدّلت أي بيانات في هذا الفحص.

## إعادة الفحص بعد الإتاحة

بعد نشر إعادة مزامنة بالإصدار `0777c350`، تكرر الفحص بعد مهلة 45 ثانية ثم بعد مهلة إضافية قدرها دقيقتان مع ترويسة `Cache-Control: no-cache`. ظل النطاق يقدّم `assets/index-BhhRtEYs.js`، وهو أصل أقدم لا يحتوي مسار `/communities/create` أو التسمية «إنشاء مجتمع»، في حين ينتج البناء المحلي المحفوظ `assets/index-nAo5FQuo.js` وفيه المسار والتسمية. كما أظهرت رؤوس استجابة HTML تاريخ تعديل 03:41 رغم بدء حاوية جديدة عند 04:08.

هذا اختلاف نشر/أصول خارج كود الواجهة؛ لا يعد المسار متاحاً إنتاجياً، ولا ينبغي مطالبة المستخدم بتسجيل الدخول أو إنشاء بيانات تجريبية حتى تنتقل البوابة إلى الحزمة الجديدة.

## وصول الحزمة الجديدة

عند 15:16 في 23 أغسطس 2026، أعاد النطاق حزمة جديدة هي `assets/index-D8vH3HTq.js`. فُتح المسار `https://yemnacommuni-mhbuucvz.manus.space/communities/create?release=latest` في Chromium مستقل؛ لم يعد إلى صفحة «غير متاح»، بل دخل حراسة الصفحة وعرض «جارٍ تحميل الحساب… / يجري التحقق من جلسة الحساب…». لا توجد جلسة مصادقة في نافذة التحقق، لذلك لم يُنشأ مجتمع ولم يُختبر النموذج أو الإرسال بعد.

## إنشاء المجتمع وتفاصيله

في 2026-08-23، بعد تأكيد جلسة المستخدم المصادقة، أُنشئ مجتمع عام تجريبي من نموذج `/communities/create` بعقد REST الحي: «مجتمع تحقق يمنا 20260823» ورابطه القصير `yemna-link-check-20260823`. انتقل التطبيق بعد نجاح الإرسال إلى `/community/cmt5ygcgr0005s601yn44hw99`، وعرض الاسم والوصف والخصوصية العامة وعضواً واحداً وصلاحية المالك. لم تُعدّل أي بيانات مجتمع قائمة.

بعد الإنشاء فُتحت صفحة البحث في الجلسة نفسها وأُدخل الاسم الكامل «مجتمع تحقق يمنا 20260823»؛ ظهرت حالة «يجري البحث…» بنجاح، ويستكمل الفحص بقراءة نتيجة المجتمع ورابط التفاصيل دون أي تعديل إضافي.

اكتمل التحقق في الإنتاج: أعاد البحث نتيجة واحدة للمجتمع التجريبي وبها رابط «فتح المجتمع» إلى `/community/cmt5ygcgr0005s601yn44hw99`. فُتح الرابط في المتصفح نفسه وعرض صفحة التفاصيل الاسم والوصف والخصوصية العامة وعضواً واحداً وحالة «أنت مالك هذا المجتمع». يثبت ذلك مسار الإنشاء ثم الظهور في البحث ثم الانتقال إلى التفاصيل بعقد REST حي.

## إعادة تحقق رابط عضو المجتمع

عند 15:27 في 23 أغسطس 2026، أعاد الإصدار `1b36a057` صفحة المجتمع التجريبي بنجاح، لكنه أظهر عضو المالك «زكريا» برابط `/profile/null` رغم نشر إصلاح تراجع المعرّف. لا يُعد إصلاح الرابط مكتملًا؛ يلزم مراجعة الشكل الفعلي لبيانات عضو المجتمع لأن الاستجابة لا تضع الاسم الظاهر في الحقل المتوقع في الواجهة.

أظهر فحص DOM اللاحق تحميل `assets/index-D8vH3HTq.js` مع بقاء الرابط `/profile/null`. لذلك وصل الإصدار الجديد فعلاً، والمطلوب هو تصحيح استخراج المعرف من بيانات العضوية الحية لا إعادة نشر فقط.

## تحقق الإصلاح المنشور

بعد نشر الإصدار `2a8cc334` في 23 أغسطس 2026، فُتحت صفحة المجتمع التجريبي نفسها داخل الجلسة المصادقة. ظهر المالك «زكريا» برابط حي إلى `/profile/cmt0znnse0004s6014l0dh8xo` بدلاً من `/profile/null`، مع بقاء وصف «عضو بلا اسم مستخدم» صادقاً لأن الاسم المستخدم في الاستجابة فارغ فعلاً.

فُتح الرابط مباشرةً في المتصفح ووصل إلى صفحة ملف «زكريا» التي عرضت عنوان الملف ومحتواه بدلاً من صفحة خطأ. يثبت ذلك أن تفاصيل المجتمع تستخدم الاسم المستخدم إن وجد، ثم معرّف المستخدم المتداخل أو معرّف العضوية عند غيابه، ولا تنشئ رابطاً عندما لا يتاح أي معرّف صالح.
