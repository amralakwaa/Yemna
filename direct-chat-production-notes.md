# تحقق إنتاج مسار المحادثة الفردية

## النتيجة الحالية

نُشر الإصدار `77f798c5` بعد اجتياز التحقق المحلي. عند فحص `/messages/chat?release=77f798c5` داخل جلسة المستخدم المصادقة، بقيت البوابة تقدّم الحزمة `assets/index-DGHgCmVE.js` السابقة، ولذلك ظهرت واجهة «هذه الشاشة قيد الربط» بدلاً من `RealtimeMessagesPage`.

المصدر المحلي والبناء اجتازا 41 مجموعة و194 اختباراً، ويتضمنان توجيه `/messages/chat` إلى واجهة الرسائل الحية. لا يُعد ذلك تحققاً إنتاجياً ناجحاً إلى أن تتبدل الحزمة المنشورة ثم يعاد الفحص. لا يغيّر هذا التوثيق أي بيانات أو محادثات للمستخدمين.
