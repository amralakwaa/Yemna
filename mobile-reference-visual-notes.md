# فحص تصميم الجوال المرجعي

- تاريخ الفحص: 2026-08-22.
- المسار `/` أصبح يعرض رأس الجوال والقصص ومنشئ المنشور وبطاقات المحتوى بكثافة أقرب إلى المرجع.
- عولجت الحاويات البنيوية بطبقة هاتف صريحة: لا تتجاوز صفحات المحتوى عرض الإطار، وتتحول واجهة الرسائل إلى عمود واحد آمن، وتنهار شبكات المجموعات والإشعارات والملف الشخصي دون أعمدة سطح مكتب.
- أظهر فحص مبكر عند عرض 390px أن المسارات `/messages` و`/groups` و`/notifications` و`/profile` كانت تُرسم كعمود ضيق عند الحافة اليمنى مع مساحة فارغة واسعة. عولج السبب لاحقاً بطبقة الهاتف النهائية، وتؤكد اللقطات الموثقة أدناه أن هذه المسارات تُملأ بعرض الهاتف في حالتي الضيف المتاحتين للفحص الآلي.
- يوجد اختبار عقد آلي يحرس محددات الهاتف وقاعدة العمود الواحد للرسائل، لكنه لا يكفي وحده لإثبات صحة العرض المرئي ويجب توسيعه بعد معالجة السبب الفعلي.
- فحص المتصفح الآلي في بيئة التطوير كشف أن نافذة المعاينة نفسها تعمل بعرض CSS كبير جداً خارج محاكاة هاتف حقيقية؛ لذا لا تُعامل لقطاتها المقصوصة كتأكيد لخلل مستخدم نهائي ولا كتأكيد لنجاح الإصلاح. يبقى التحقق النهائي مطلوباً على جهاز أو محاكي هاتف فعلي.

## محاكاة هاتف CSS مستقلة

- استُخدم Chromium مستقل بعرض CSS حقيقي `390×844` وقياس بكسل `1`، تفادياً لتحجيم أداة المعاينة الذي يضخم العرض المحسوب.
- يظهر مسار **المجموعات** بعرض كامل ومن دون قص أفقي: رأس سياقي، حقل بحث، فلتر، وحالة فارغة حقيقية عند غياب بيانات مجتمعات.
- يظهر مسار **الرسائل** بعرض كامل ومن دون قص أفقي: رأس سياقي، حالة وصول صريحة للمستخدم غير المسجل، وزر تسجيل دخول. لا يمكن تدقيق قائمة محادثات فعلية دون جلسة مستخدم، ولم تُفبرك بيانات بديلة لأجل اللقطة.

### المجتمعات الحية بعد إزالة البيانات التقديرية

- في محاكي هاتف مستقل `390×844`، ظهر مسار `/communities` بعرض كامل ومن دون قص أفقي أو تقديرات محافظات أو أزرار موقع شكلية.
- بعد اكتمال تحميل REST، ظهرت حالة فراغ حقيقية عند عدم وجود مجتمعات: «لا توجد مجتمعات بعد»، بدلاً من محتوى ثابت أو تقديرات.
- شريط التنبيه السفلي الذي ظهر في لقطة المحاكي تابع لبيئة المعاينة وليس من واجهة التطبيق.

### الإشعارات والملف الشخصي — حالة الضيف (390×844)

- ظهر مسارا `/notifications` و`/profile` داخل عرض الهاتف كاملاً، برأس سياقي واحد وعناصر تحكم مرئية من دون تجاوز أفقي.
- لا تعرض أي من الصفحتين بيانات حساب بديلة أو تقديرية عند غياب الجلسة؛ بدلاً من ذلك تعرض كل صفحة رسالة دخول وإجراء «تسجيل الدخول» واضحاً.
- لا يمكن اعتماد هذه اللقطات كفحص لمحتوى الحساب أو الإشعارات الحقيقية لأن المحاكي يعمل بلا جلسة مستخدم؛ يجب اختبار تلك الحالة بحساب قائم قبل الادعاء بتحققها الحي.

### الرسائل — حالة الضيف (390×844)

- يطابق رأس `/messages` بنية المرجع الأساسية: قائمة يساراً، عنوان سياقي في الوسط، وزر إنشاء رسالة يميناً، مع عرض كامل بلا قص أفقي.
- تم التحقق من حالة الضيف فقط؛ تظهر رسالة دخول صادقة ولا تعرض محادثات افتراضية. تتطلب قائمة المحادثات الفعلية فحصاً ضمن جلسة مستخدم قائمة.
- حجبت رسالة بيئة المعاينة الخارجية أسفل اللقطة جزءاً من شريط التنقل السفلي؛ لا تُنسب هذه الرسالة إلى المنتج ولا يمكن الحكم من خلالها على آخر عناصر الشريط.

### فحص نطاق الإنتاج المنشور (390×844)

- حمّل المسار `/` على النطاق المنشور برأس يمنا، منشئ المنشور، حالة الضيف الصادقة، وشريط تنقل سفلي؛ لم يظهر شريط تحذير المعاينة الخارجي الذي ظهر في بيئة التطوير.
- ظهرت أول لقطة لـ`/profile` فارغة قبل اكتمال بدء التطبيق. أُعيد الاختبار بمهلة إتاحة أطول، فظهرت حالة ضيف صادقة بعنوان «الملف الشخصي» وزر تسجيل الدخول وشريط التنقل السفلي؛ لذا لم يثبت وجود خلل وظيفي في المسار. تبقى بيانات الحساب الفعلية غير قابلة للفحص دون جلسة مستخدم قائمة.
- حمّل `/communities` قائمة REST الحية على الإنتاج، مع رأس سياقي وعلامات تبويب ونتيجة فراغ صادقة «لا توجد مجتمعات بعد» بدلاً من بطاقات أو محافظات تقديرية. لا يمكن فحص قائمة مجتمعات فعلية قبل وجود سجلات متاحة من قاعدة البيانات لهذه الجلسة.
- حمّل `/notifications` حالة ضيف صادقة بعنوان سياقي واحد وزر تسجيل الدخول، بلا قص أفقي. لا يمكن التحقق من عناصر الإشعارات الفعلية دون جلسة مستخدم قائمة.
- حمّل `/groups` دليلاً هاتفياً برأس «المجموعات» وزر إنشاء وبحث ومرشح وحالة فراغ صادقة من REST، بلا تجاوز أفقي. تعذر فحص بطاقات مجموعة فعلية لعدم توفر مجموعة مرئية لهذه الجلسة.
- حمّل `/messages` رأساً قريباً من المرجع (قائمة، عنوان الرسائل، إنشاء رسالة) وحالة ضيف صادقة، بلا قص أفقي. لا يمكن فحص دليل المستخدمين والمحادثات الفعلية من دون جلسة مستخدم قائمة.

### فرق متابعة في الخلاصة المنشورة (390×844)

- يتوافق تموضع رأس الخلاصة في حالة الضيف مع المرجع من حيث القائمة اليسرى والشعار الوسطي والبحث والإشعارات يميناً. لكن شريط إجراءات منشئ المنشور مضغوط لدرجة أن تسمية الإجراء الأول مقصوصة في اللقطة؛ سُجل كخلل واجهة مستقل لإصلاحه.
- وسم «Made with Manus» أسفل اللقطة طبقة بيئة استضافة خارج واجهة المنتج، وهو يحجب جزئياً بعض عناصر التنقل في لقطة الإنتاج؛ لا يمكن ولا ينبغي اعتباره عنصراً من تطبيق يمنا أو إصلاحه من CSS التطبيق.

### متابعة إصلاح منشئ المنشور (محاكي مستقل، 390×844)

- عولج انضغاط شريط الإجراءات عبر شبكة هاتفية ذات أعمدة مضبوطة، مع تجاوز صريح للقاعدة الأقدم التي أخفت تسميات الأزرار. تظهر الآن تسميات «صورة / فيديو»، «رفع فيديو»، «مشاعر / نشاط»، «موقع»، و«نشر» داخل عرض الهاتف بلا قص أفقي.
- اجتاز اختبار عقد الهاتف المحدث 146 اختباراً في 37 ملف اختبار. يتطلب تثبيت هذا الإصلاح في الإنتاج بناءً جديداً وحفظ checkpoint لاحقاً.

### دليل المجموعات بعد مواءمة التبويبات (محاكي مستقل، 390×844)

- استُبدل تبويب «الكل» المنفرد بشريط واضح من «استكشف» و«مجموعاتي»، بمحاذاة بنية التبويبات في المرجع ودون تجاوز أفقي. تظهر الخلاصة الحية وحالة الفراغ الصادقة في تبويب الاستكشاف؛ اللقطة الحالية لا تحوي مجتمعات منشأة في قاعدة التطوير.
- تبويب «مجموعاتي» متصل بمسار عضوية محمي جديد (`GET /communities/mine`) ولا يعرض قائمة مصطنعة: في حالة الضيف يطلب تسجيل الدخول، وبعد الجلسة يعرض العضويات الفعلية أو حالة فراغ صادقة. اجتازت الاختبارات الكاملة 147 حالة وبناء الإنتاج، ولم يُنفذ تفاعل عضوية حي لأن المحاكي لا يستخدم جلسة مستخدم.

### حالة التنقل السفلي (محاكي مستقل، 390×844)

- عند فتح `/friends` ينتقل التمييز البورغندي إلى أيقونة وتسمية «الأصدقاء»، وعند فتح `/notifications` ينتقل إلى «الإشعارات»؛ بقي زر الإنشاء المركزي وقائمة الهاتف ظاهرين بالترتيب المرجعي نفسه.
- أضيفت دلالة `aria-current="page"` للوجهات المرتبطة بالمسار في الشريط السفلي. اللقطتان أثبتتا التغيير في حالة الضيف، بينما محتوى الأصدقاء والإشعارات نفسه يظل صادقاً في طلب تسجيل الدخول إلى بيانات الحساب.

### إعادة تحقق الإنتاج المنشور من التنقل السفلي (390×844)

- أكدت لقطة `/friends` المنشورة أن التمييز البورغندي يصل إلى «الأصدقاء» بعد النشر التلقائي. حملت `/notifications` حالة ضيف سليمة ورأساً سياقياً صحيحاً، لكن طبقة «Made with Manus» الخارجية حجبت عنصر الإشعارات نفسه في لقطة الإنتاج؛ لذلك لا يصح اعتبار لون الأيقونة على هذا المسار مؤكداً بصرياً في هذه اللقطة تحديداً.
- تغطية الاختبار المعزول تثبت من المصدر أن `/notifications` يحصل على الصنف النشط و`aria-current`، بينما يحتاج أي تأكيد بصري كامل للعنصر المحجوب إلى بيئة عرض لا تضع طبقة الاستضافة فوق التنقل.

### نطاق المراجع المرفقة

- يوضح المرجع التجميعي `1000582440.png` أن النطاق المستهدف ليس لقطة واحدة؛ بل **32 شاشة هاتفية رئيسية** موزعة على التسجيل والتحقق، الخلاصة والاكتشاف، الملف والمحتوى، القصص والمجموعات والرسائل، ثم الإعدادات والدعم والمزيد.
- المرجع العريض `1000582431.png` يثبت لغة التصميم العامة لسطح المكتب: البورغندي كلون تفاعل رئيسي، القصص في البداية، منشئ منشور متعدد الإجراءات، بطاقة منشور، وقوائم جانبية للسياقات الاجتماعية. لذا لا يُعامل تحقق عدد قليل من مسارات حالة الضيف كإثبات لاكتمال جميع الشاشات الـ32؛ يلزم تدقيق شاشة-بشاشة وتسجيل الحالة الفعلية لكل واحدة.
- يضيف المرجع `1000582441.png` شاشات المجموعات والصفحات والفعاليات، ثم الملفات والوسائط والحفظ والعلاقات، ثم الخصوصية والدعم والإبلاغ وتأكيد الحذف. يضيف `1000582442.png` إدارة المشرف: دخول الإدارة، الإحصاءات، المستخدمون والبلاغات والمحتوى والمجموعات والصفحات، وسجلات النظام والنسخ الاحتياطي ووضع الصيانة.
- هذه الشاشات تمثل نطاقاً وظيفياً أوسع من إصلاح التخطيط؛ وجود مسار أو بطاقة في التطبيق لا يثبت اتساقه مع المرجع أو ارتباطه بالبيانات الفعلية. ستُفحص الخرائط والمسارات القائمة قبل وضع أي ادعاء باكتمالها.
- يضيف المرجع `1000582443.png` تدفقاً كاملاً للذكاء الاصطناعي (مساعد، اقتراح منشور، تلخيص، تحليل مشاعر، تصنيف، بحث، كتابة، توصيات، وتقارير)، بينما يفصل `1000582450.png` البحث العام والمتقدم حسب الأشخاص والمجموعات والصفحات والمنشورات والفعاليات والمواقع والهاشتاقات، مع الاكتشاف والبحث الصوتي والحفظ.
- المسارات موجودة في خريطة التطبيق، لكن مستوى التوصيل إلى بيانات حية وقدرات خادم حقيقية يختلف حسب الشاشة؛ تُفصل هذه الحقيقة عن مجرد وجود المسار عند تقييم الإنجاز.
- يوسع المرجعان `1000582453.png` و`1000582455.png` نطاق البحث إلى نتائج متعددة الوسائط، تصفية متقدمة، بحث محلي، اهتمامات، أحداث رائجة، حفظ وتنبيهات، ثم يحددان أيضاً شاشات صندوق الرسائل والمحادثة الفردية والجماعية وبيانات المحادثة وإنشاء المجموعة وسجل المكالمات والمكالمة وطلبات الرسائل.
- يوجد تنفيذ حقيقي للرسائل المباشرة في `RealtimePages.tsx`، لكن المكالمات والمجموعات والبحث المتقدم تحتاج تدقيقاً مستقلاً؛ لا تُساوى صور مرجع التصميم بوجود تكامل اتصالات أو بيانات حية لمجرد أن المسار مرسوم.
- يحدد `1000582538.png` التفاصيل المرجعية للملف الشخصي والعلاقات: ملف المالك والعام، تعديل الملف، الأصدقاء والطلبات والمتابعون والمتابَعون والمشتركون والحظر، ثم المحفوظات والألبومات والصور والفيديوهات والتفاعل والإشارات والذكريات. يحدد `1000582543.png` تفاصيل استكشاف وإدارة المجموعات والصفحات والمجتمعات المحلية وطلبات الانضمام والأعضاء والإشعارات.
- هذه الجداول تؤكد أن المراجعة لا تكتفي بمسار `/profile` أو `/groups` العام؛ يلزم تدقيق تبويبات العلاقات والوسائط ومسارات الإدارة الفرعية على نحو منفصل وبالبيانات الحقيقية حيث تكون الجلسة متاحة.
- يضيف `1000582546.png` المتغيرات الأكثر تفصيلاً للمجموعات والصفحات والمجتمعات المحلية وإدارتها، في حين يفصل `1000582554.png` الإعدادات والخصوصية والأمان، بما فيها رؤية المنشورات والرسائل والجلسات وكلمة المرور والتحقق وخيارات التنبيه والحذف ومركز الخصوصية والبيانات ومعلومات الحساب.
- تتطلب هذه الشاشات حذراً وظيفياً خاصاً: أي أزرار تنفذ تغييراً للحساب أو للخصوصية يجب ألا تصبح واجهة شكلية، بل تُوصل بخادم محمي وتتحقق من الجلسة، أو تبقى صريحة بأن الإعداد لا يزال غير متاح.
- يحدد `1000582561.png` تدفق إنشاء المنشور وإضافة الصورة أو الفيديو وتحرير الصورة ومعرض الوسائط والقصص والتفاعلات وخيارات المنشور والإبلاغ. يحدد `1000582568.png` مركز الإشعارات وتفاصيل أنواع الإشعار المختلفة، وطلبات الصداقة، وإعدادات وتفاصيل الإشعارات.
- توجد أجزاء تشغيلية من هذه المجالات في التطبيق، لكن التحقق السابق في حالة الضيف لا يثبت أدوات التحرير أو التفاعل أو معاني التنبيه الشخصية؛ هذه الأجزاء تحتاج جلسة واختبارات تدفق مخصصة قبل وصفها بأنها مكتملة.
- يعرض `1000582569.png` تدفق Reels والبث المباشر الكامل: استكشاف ومشاهدة وإنشاء وتحرير وصوت وفئات للبكرة، ثم بث مباشر ومشاهدته ومعلوماته وإنشاؤه وإدارته وسجله. يعرض `1000582570.png` حالات التحميل والفراغ والخطأ وعدم الاتصال والحساب المحظور والمحتوى أو المستخدم غير المتاح وفشل رفع الصور والفيديو.
- لا توجد قاعدة للتحقق من إتمام Reels أو البث أو المكالمات في النسخة الحالية، ولذلك تُعامل بوصفها نطاقات مواصفات مرجعية متبقية وليست وظائف مكتملة. حالات التحميل والخطأ المبنية لكل طلب بيانات تبقى منفصلة عن تغطية جميع حالات الاستثناء في المرجع.
- يغطي `1000582571.png` المجتمع اليمني المتخصص: المحافظات والمدن والمجتمعات المحلية والجامعات والاهتمامات واكتشاف مجتمع قريب، ثم صفحة المجتمع وإنشاؤه وطلبات الانضمام وأعضاؤه وإدارته ومعلوماته. يغطي `LiveCommunitiesPage` اكتشاف المجتمعات الحية حالياً، لكن بقية تفاصيل التصنيف والإنشاء والإدارة والعضويات تمثل نطاقاً أكبر يحتاج خدمات وإذن وصول واختبارات مستقلة.

### ملاحظة تحقق محلي للحساب (قيد الإعادة)

- تعذر فحص `/saved` و`/photos` في لقطة محاكي الهاتف لأن منفذ التطوير المحلي رفض الاتصال (`ERR_CONNECTION_REFUSED`) بعد اكتمال الفحص السابق. هذه ليست نتيجة للصفحتين ولا يمكن استخدامها للحكم على بياناتهما؛ يلزم إعادة تشغيل خادم التطوير ثم تكرار الفحص.
- بعد إعادة تشغيل الخادم من المنفذ الفعلي، ظهرت `/photos` بحالة ضيف سليمة تحتوي دعوة تسجيل الدخول ولا تعرض صوراً مصطنعة. لكن `/saved` بقيت في شبكة تحميل هيكلية رمادية رغم غياب الجلسة، وهو خلل حالة حقيقي سُجل للإصلاح؛ لا تُعد المحفوظات متحققة بعد.
- بعد الإصلاح، ظهرت `/saved` على محاكي 390×844 برأس سياقي وشريط تنقل سليمين، مع أيقونة محفوظات ودعوة واضحة إلى تسجيل الدخول؛ لم تعد أي شبكة وسائط أو عناصر ثابتة ظاهرة للضيف. وُضع للمستخدم المسجل أيضاً فراغ صريح بدلاً من الشبكة المصطنعة إلى حين توفير مصدر محفوظات حقيقي.

### تدقيق وسائط الحساب بحالة الضيف (390×844)

- حمّل `/albums` و`/videos` بطبقات تعريفية مناسبة للمسار ثم دعوة تسجيل دخول صريحة، من دون بطاقات ألبوم أو صور أو فيديوهات مصطنعة. ما يظهر من عنوان ووصف المسار بنية واجهة لا يمثل بيانات مستخدم.
- بقي زر «إضافة وسائط» ظاهراً في الألبومات مع غياب الجلسة؛ لا يثبت هذا الفحص أن مسار الرفع متاح للضيف. يلزم عند تدقيق تدفق الرفع بحساب حقيقي التحقق من انتقاله إلى المصادقة أو منعه بوضوح قبل وصفه بأنه مكتمل.

### إصلاح سجل النشاط المصطنع (محاكي مستقل، 390×844)

- استُبدلت عناصر التفاعل الثابتة في `/activity` بحالة ضيف صادقة: عنوان «تفاعلاتك»، رمز التفاعل، وصف يبين البيانات التي ستظهر بعد المصادقة، وزر تسجيل الدخول. لم تعد أي أسماء منشورات أو أزمنة أو تفاعلات تقديرية ظاهرة.
- اجتاز عقد البيانات الحية المحدث 6/6، بما فيها حماية مستقلة لمسار التفاعلات. لم يُختبر عرض تفاعلات الحساب الفعلية لأن المحاكي لا يستخدم جلسة مستخدم.

### تدقيق علاقات الحساب للضيف (محاكي مستقل، 390×844)

- لم تظهر بطاقات أشخاص أو طلبات مصطنعة في `/followers` و`/friend-requests`، ولم يظهر قص أفقي؛ لكن الصفحتين عرضتا عداد `0` وحالة «لا توجد بيانات حالياً» من دون إيضاح أن غياب الجلسة هو السبب. هذا لا يثبت بيانات حساب حقيقية، وسُجل حارس ضيف موحد لهذه المسارات كتصحيح لازم.
- بعد الإصلاح، تعرض `/followers` و`/friend-requests` في 390×844 عنوانهما السياقي الصحيح، ورمز العلاقات، ورسالة مصادقة واضحة وزر تسجيل الدخول، من دون عداد صفر أو بطاقات مصطنعة. اجتاز عقد البيانات الحية 7/7؛ تدفق العلاقات مع حساب مسجل لم يُختبر هنا لعدم استخدام جلسة متصفح.

### حماية ألبومات الضيف (محاكي مستقل، 390×844)

- بعد تصحيح منفذ التطوير إلى `3001`، ظهرت `/albums` بعنوان الألبومات وحالة دعوة تسجيل دخول صادقة فقط. لا يظهر زر «إضافة وسائط» ولا شبكة أو صور مصطنعة للضيف. كانت لقطة الاتصال المرفوض السابقة ناتجة عن استخدام المنفذ `3000` بعد إعادة التشغيل، ولا تُعامل كنتيجة واجهة.
- فُحص المسار المباشر `/media/upload` للضيف أيضاً؛ يعرض «هذه الصفحة غير متاحة» ولا يعرض واجهة رفع أو حقل ملفات. بذلك لا يوجد طريق ظاهر أو مباشر لبدء رفع وسائط شخصية قبل المصادقة.
