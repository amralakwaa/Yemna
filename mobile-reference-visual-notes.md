# فحص تصميم الجوال المرجعي

- تاريخ الفحص: 2026-08-22.
- المسار `/` أصبح يعرض رأس الجوال والقصص ومنشئ المنشور وبطاقات المحتوى بكثافة أقرب إلى المرجع.
- عولجت الحاويات البنيوية بطبقة هاتف صريحة: لا تتجاوز صفحات المحتوى عرض الإطار، وتتحول واجهة الرسائل إلى عمود واحد آمن، وتنهار شبكات المجموعات والإشعارات والملف الشخصي دون أعمدة سطح مكتب.
- أظهر فحص مبكر عند عرض 390px أن المسارات `/messages` و`/groups` و`/notifications` و`/profile` كانت تُرسم كعمود ضيق عند الحافة اليمنى مع مساحة فارغة واسعة. عولج السبب لاحقاً بطبقة الهاتف النهائية، وتؤكد اللقطات الموثقة أدناه أن هذه المسارات تُملأ بعرض الهاتف في حالتي الضيف المتاحتين للفحص الآلي.
- يوجد اختبار عقد آلي يحرس محددات الهاتف وقاعدة العمود الواحد للرسائل، لكنه لا يكفي وحده لإثبات صحة العرض المرئي ويجب توسيعه بعد معالجة السبب الفعلي.
- فحص المتصفح الآلي في بيئة التطوير كشف أن نافذة المعاينة نفسها تعمل بعرض CSS كبير جداً خارج محاكاة هاتف حقيقية؛ لذا لا تُعامل لقطاتها المقصوصة كتأكيد لخلل مستخدم نهائي ولا كتأكيد لنجاح الإصلاح. يبقى التحقق النهائي مطلوباً على جهاز أو محاكي هاتف فعلي.

## محاكاة هاتف CSS مستقلة

- استُخدم Chromium مستقل بعرض CSS حقيقي `390×844` وقياس بكسل `1`، تفادياً لتحجيم أداة المعاينة الذي يضخم العرض المحسوب.
- يظهر مسار **المجموعات** بعرض كامل ومن دون قص أفقي: رأس سياقي، حقل بحث، فلتر، وحالة فارغة حقيقية عند غياب بيانات مجتمعات.
- يظهر مسار **الرسائل** بعرض كامل ومن دون قص أفقي: رأس سياقي، حالة وصول صريحة للمستخدم غير المسجل، وزر تسجيل دخول. لا يمكن تدقيق قائمة محادثات فعلية دون جلسة مستخدم، ولم تُفبرك بيانات بديلة لأجل اللقطة.

### المجتمعات الحية بعد إزالة البيانات التقديرية

- في محاكي هاتف مستقل `390×844`، ظهر مسار `/communities` بعرض كامل ومن دون قص أفقي أو تقديرات محافظات أو أزرار موقع شكلية.
- بعد اكتمال تحميل REST، ظهرت حالة فراغ حقيقية عند عدم وجود مجتمعات: «لا توجد مجتمعات بعد»، بدلاً من محتوى ثابت أو تقديرات.
- شريط التنبيه السفلي الذي ظهر في لقطة المحاكي تابع لبيئة المعاينة وليس من واجهة التطبيق.

### الإشعارات والملف الشخصي — حالة الضيف (390×844)

- ظهر مسارا `/notifications` و`/profile` داخل عرض الهاتف كاملاً، برأس سياقي واحد وعناصر تحكم مرئية من دون تجاوز أفقي.
- لا تعرض أي من الصفحتين بيانات حساب بديلة أو تقديرية عند غياب الجلسة؛ بدلاً من ذلك تعرض كل صفحة رسالة دخول وإجراء «تسجيل الدخول» واضحاً.
- لا يمكن اعتماد هذه اللقطات كفحص لمحتوى الحساب أو الإشعارات الحقيقية لأن المحاكي يعمل بلا جلسة مستخدم؛ يجب اختبار تلك الحالة بحساب قائم قبل الادعاء بتحققها الحي.

### الرسائل — حالة الضيف (390×844)

- يطابق رأس `/messages` بنية المرجع الأساسية: قائمة يساراً، عنوان سياقي في الوسط، وزر إنشاء رسالة يميناً، مع عرض كامل بلا قص أفقي.
- تم التحقق من حالة الضيف فقط؛ تظهر رسالة دخول صادقة ولا تعرض محادثات افتراضية. تتطلب قائمة المحادثات الفعلية فحصاً ضمن جلسة مستخدم قائمة.
- حجبت رسالة بيئة المعاينة الخارجية أسفل اللقطة جزءاً من شريط التنقل السفلي؛ لا تُنسب هذه الرسالة إلى المنتج ولا يمكن الحكم من خلالها على آخر عناصر الشريط.

### فحص نطاق الإنتاج المنشور (390×844)

- حمّل المسار `/` على النطاق المنشور برأس يمنا، منشئ المنشور، حالة الضيف الصادقة، وشريط تنقل سفلي؛ لم يظهر شريط تحذير المعاينة الخارجي الذي ظهر في بيئة التطوير.
- ظهرت أول لقطة لـ`/profile` فارغة قبل اكتمال بدء التطبيق. أُعيد الاختبار بمهلة إتاحة أطول، فظهرت حالة ضيف صادقة بعنوان «الملف الشخصي» وزر تسجيل الدخول وشريط التنقل السفلي؛ لذا لم يثبت وجود خلل وظيفي في المسار. تبقى بيانات الحساب الفعلية غير قابلة للفحص دون جلسة مستخدم قائمة.
- حمّل `/communities` قائمة REST الحية على الإنتاج، مع رأس سياقي وعلامات تبويب ونتيجة فراغ صادقة «لا توجد مجتمعات بعد» بدلاً من بطاقات أو محافظات تقديرية. لا يمكن فحص قائمة مجتمعات فعلية قبل وجود سجلات متاحة من قاعدة البيانات لهذه الجلسة.
- حمّل `/notifications` حالة ضيف صادقة بعنوان سياقي واحد وزر تسجيل الدخول، بلا قص أفقي. لا يمكن التحقق من عناصر الإشعارات الفعلية دون جلسة مستخدم قائمة.
- حمّل `/groups` دليلاً هاتفياً برأس «المجموعات» وزر إنشاء وبحث ومرشح وحالة فراغ صادقة من REST، بلا تجاوز أفقي. تعذر فحص بطاقات مجموعة فعلية لعدم توفر مجموعة مرئية لهذه الجلسة.
- حمّل `/messages` رأساً قريباً من المرجع (قائمة، عنوان الرسائل، إنشاء رسالة) وحالة ضيف صادقة، بلا قص أفقي. لا يمكن فحص دليل المستخدمين والمحادثات الفعلية من دون جلسة مستخدم قائمة.

### فرق متابعة في الخلاصة المنشورة (390×844)

- يتوافق تموضع رأس الخلاصة في حالة الضيف مع المرجع من حيث القائمة اليسرى والشعار الوسطي والبحث والإشعارات يميناً. لكن شريط إجراءات منشئ المنشور مضغوط لدرجة أن تسمية الإجراء الأول مقصوصة في اللقطة؛ سُجل كخلل واجهة مستقل لإصلاحه.
- وسم «Made with Manus» أسفل اللقطة طبقة بيئة استضافة خارج واجهة المنتج، وهو يحجب جزئياً بعض عناصر التنقل في لقطة الإنتاج؛ لا يمكن ولا ينبغي اعتباره عنصراً من تطبيق يمنا أو إصلاحه من CSS التطبيق.

### متابعة إصلاح منشئ المنشور (محاكي مستقل، 390×844)

- عولج انضغاط شريط الإجراءات عبر شبكة هاتفية ذات أعمدة مضبوطة، مع تجاوز صريح للقاعدة الأقدم التي أخفت تسميات الأزرار. تظهر الآن تسميات «صورة / فيديو»، «رفع فيديو»، «مشاعر / نشاط»، «موقع»، و«نشر» داخل عرض الهاتف بلا قص أفقي.
- اجتاز اختبار عقد الهاتف المحدث 146 اختباراً في 37 ملف اختبار. يتطلب تثبيت هذا الإصلاح في الإنتاج بناءً جديداً وحفظ checkpoint لاحقاً.

### دليل المجموعات بعد مواءمة التبويبات (محاكي مستقل، 390×844)

- استُبدل تبويب «الكل» المنفرد بشريط واضح من «استكشف» و«مجموعاتي»، بمحاذاة بنية التبويبات في المرجع ودون تجاوز أفقي. تظهر الخلاصة الحية وحالة الفراغ الصادقة في تبويب الاستكشاف؛ اللقطة الحالية لا تحوي مجتمعات منشأة في قاعدة التطوير.
- تبويب «مجموعاتي» متصل بمسار عضوية محمي جديد (`GET /communities/mine`) ولا يعرض قائمة مصطنعة: في حالة الضيف يطلب تسجيل الدخول، وبعد الجلسة يعرض العضويات الفعلية أو حالة فراغ صادقة. اجتازت الاختبارات الكاملة 147 حالة وبناء الإنتاج، ولم يُنفذ تفاعل عضوية حي لأن المحاكي لا يستخدم جلسة مستخدم.

### حالة التنقل السفلي (محاكي مستقل، 390×844)

- عند فتح `/friends` ينتقل التمييز البورغندي إلى أيقونة وتسمية «الأصدقاء»، وعند فتح `/notifications` ينتقل إلى «الإشعارات»؛ بقي زر الإنشاء المركزي وقائمة الهاتف ظاهرين بالترتيب المرجعي نفسه.
- أضيفت دلالة `aria-current="page"` للوجهات المرتبطة بالمسار في الشريط السفلي. اللقطتان أثبتتا التغيير في حالة الضيف، بينما محتوى الأصدقاء والإشعارات نفسه يظل صادقاً في طلب تسجيل الدخول إلى بيانات الحساب.
