# التحقق المرئي من القصص

تم فحص المسار `/story` على سطح المكتب بقياس `1280×720` بعد تشغيل وحدة القصص. تظهر حالة عدم وجود قصص داخل إطار عمودي مركّز بخلفية داكنة، مع عنوان ورسالة وزر إنشاء قصة في منتصف الإطار. يعرض هذا الفحص حالة فارغة فقط لأن بيئة المعاينة لم تتضمن جلسة مستخدم أو قصة منشورة.
